# ── 数据目录（不上传）──────────────────────────────────
data/

# ── 文档目录（不上传）──────────────────────────────────
docs/

# ── 本地日志（不上传）──────────────────────────────────
logs/
run_logs/

# ── 权重文件（目录保留，文件不上传）────────────────────
weights/*.bin
weights/*.pth
weights/*.pt
weights/*.safetensors

# ── 训练输出 ────────────────────────────────────────────
checkpoints/

# ── Benchmark 数据、外部仓库、权重与结果（不上传）────────
benchmark_data/
benchmark_outputs/
baseline_weights/
external_repos/

# ── Python 缓存 ─────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd

# ── Jupyter ─────────────────────────────────────────────
.ipynb_checkpoints/

# ── 编辑器 ──────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── 推理输出（本地测试结果，不上传）────────────────────
infer_output/

# ── Git / GitHub Token 配置（包含敏感信息，绝不上传）───
.env
.env.local
.github_token

# ── 系统文件 ────────────────────────────────────────────
.DS_Store
Thumbs.db
